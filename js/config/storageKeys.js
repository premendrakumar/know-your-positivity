/** localStorage keys used across the app */
export const STORAGE_KEYS = {
  language: "appLanguage",
  theme: "know-your-potential-theme",
  /** JSON blob: versioned app state (card positions per language, extensible) */
  appState: "kyp-app-state",
};
